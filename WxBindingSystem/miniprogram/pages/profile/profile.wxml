<!-- 个人中心页 -->
<view class="page">
  <!-- 用户信息 -->
  <view class="user-card">
    <view class="avatar">👤</view>
    <view class="user-info">
      <view class="username">{{userInfo.username || '未登录'}}</view>
      <view class="role">
        <text class="tag {{userInfo.role === 'admin' ? 'tag-info' : 'tag-success'}}">
          {{userInfo.role === 'admin' ? '管理员' : '普通用户'}}
        </text>
      </view>
    </view>
  </view>

  <!-- 菜单 -->
  <view class="menu-list">
    <view class="menu-item">
      <text class="menu-icon">ℹ️</text>
      <text class="menu-text">关于系统</text>
      <text class="menu-arrow">›</text>
    </view>
  </view>

  <!-- 退出登录 -->
  <view class="logout-area">
    <button class="btn btn-danger" bindtap="onLogout">退出登录</button>
  </view>

  <!-- 版本信息 -->
  <view class="version">
    <text>设备绑定系统 v1.0.0</text>
  </view>
</view>
